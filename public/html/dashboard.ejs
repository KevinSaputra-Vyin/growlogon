<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Growtopia Login</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #4361ee;
            --secondary: #3f37c9;
            --accent: #4cc9f0;
            --dark: #212529;
            --light: #f8f9fa;
            --success: #4bb543;
        }
        
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: transparent;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 16px;
        }
        
        .card {
            display: flex;
            max-width: 900px;
            width: 100%;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 24px;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
            overflow: hidden;
            flex-wrap: wrap;
        }
        
        .hero {
            flex: 1;
            min-width: 300px;
            padding: 40px;
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            color: white;
            display: flex;
            flex-direction: column;
            justify-content: center;
            border-radius: 24px 0 0 24px;
        }
        
        .form-section {
            flex: 1;
            min-width: 300px;
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 24px;
        }
        
        .tagline {
            font-size: 14px;
            margin-bottom: 32px;
            line-height: 1.6;
            opacity: 0.9;
        }
        
        .features {
            margin-bottom: 16px;
        }
        
        .feature {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 16px;
            font-size: 14px;
        }
        
        .feature i {
            background: rgba(255, 255, 255, 0.2);
            width: 36px;
            height: 36px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
        }
        
        .form-container {
            background: var(--light);
            padding: 32px;
            border-radius: 16px;
            margin-bottom: 24px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        }
        
        .form-header {
            display: flex;
            align-items: center;
            gap: 12px;
            color: var(--primary);
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 16px;
            justify-content: center;
        }
        
        .form-subheader {
            color: var(--dark);
            opacity: 0.7;
            font-size: 13px;
            margin-bottom: 24px;
            text-align: center;
        }
        
        .input-group {
            margin-bottom: 16px;
            position: relative;
        }
        
        .input-group label {
            display: block;
            margin-bottom: 8px;
            font-size: 13px;
            font-weight: 500;
            color: var(--dark);
            opacity: 0.8;
        }
        
        .input-icon {
            position: absolute;
            left: 14px;
            top: 38px;
            color: var(--primary);
            font-size: 16px;
        }
        
        .input-group input {
            width: 100%;
            padding: 12px 16px 12px 40px;
            border: 1px solid #e9ecef;
            border-radius: 12px;
            font-size: 14px;
            transition: all 0.3s;
            background: white;
        }
        
        .input-group input:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(67, 97, 238, 0.1);
            outline: none;
        }
        
        .btn {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            width: 100%;
            padding: 14px;
            border-radius: 12px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            border: none;
        }
        
        .btn-primary {
            background: var(--primary);
            color: white;
        }
        
        .btn-primary:hover {
            background: var(--secondary);
            transform: translateY(-2px);
        }
        
        .btn-secondary {
            background: var(--success);
            color: white;
            margin-top: 16px;
        }
        
        .btn-secondary:hover {
            background: #3a9e34;
            transform: translateY(-2px);
        }
        
        .footer {
            margin-top: 24px;
            color: var(--dark);
            opacity: 0.6;
            font-size: 12px;
            text-align: center;
        }
        
        .footer a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 500;
        }
        
        @media (max-width: 768px) {
            .card {
                flex-direction: column;
                border-radius: 16px;
            }
            
            .hero {
                border-radius: 16px 16px 0 0;
            }
            
            .form-section {
                padding: 32px 24px;
            }
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="hero">
            <div class="logo">
                <i class="fas fa-gamepad"></i>
                <span>Growtopia</span>
            </div>
            
            <div class="tagline">
                Join millions of players in this creative sandbox MMO game where you can build, trade, and explore unique worlds!
            </div>
            
            <div class="features">
                <div class="feature">
                    <i class="fas fa-cubes"></i>
                    <span>Build anything you imagine</span>
                </div>
                <div class="feature">
                    <i class="fas fa-handshake"></i>
                    <span>Trade with global community</span>
                </div>
                <div class="feature">
                    <i class="fas fa-trophy"></i>
                    <span>Compete in events</span>
                </div>
                <div class="feature">
                    <i class="fas fa-users"></i>
                    <span>Join guilds & make friends</span>
                </div>
            </div>
        </div>
        
        <div class="form-section">
            <form action="/player/growid/login/validate" method="POST">
                <div class="form-container">
                    <div class="form-header">
                        <i class="fas fa-sign-in-alt"></i>
                        <span>Welcome Back!</span>
                    </div>
                    <div class="form-subheader">
                        Sign in to access your account and continue your adventure
                    </div>
                    
                    <div class="input-group">
                        <label for="growid">GrowID</label>
                        <i class="fas fa-user input-icon"></i>
                        <input type="text" id="growid" name="growid" placeholder="Your GrowID" required>
                    </div>
                    
                    <div class="input-group">
                        <label for="password">Password</label>
                        <i class="fas fa-lock input-icon"></i>
                        <input type="password" id="password" name="password" placeholder="••••••••" required>
                    </div>
                    
                    <a href="/player/growid/login/validate" class="btn btn-primary">
                        <i class="fas fa-user-plus"></i> Register Account
                    </a>
                </div>
                
                <button type="submit" class="btn btn-secondary">
                    <i class="fas fa-play"></i> Login to Growtopia
                </button>
            </form>
            
            <div class="footer">
                <p>Powered by GTP5 Gateway</p>
                <a href="#"><i class="fas fa-question-circle"></i> Need help? Contact Support</a>
            </div>
        </div>
    </div>
</body>
</html>
